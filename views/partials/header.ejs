<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= title %></title>
  </head>
  <body>
    <nav>

      <div class="nav">

        <a href="/characters" id="charItem" <%-title ==='All Characters' ? 'class="active"' : '' %>>ALL characters</a> 
        <% if (user) { %>
          <a href="/characters/new" id="charItem" <%-title==='Add Performer' ? 'class="active"' : '' %>>ADD Character</a>

          <a href="/auth/logout" id="login">Log Out</a>

        <% } else { %>
          <a href="/auth/google" id="login">Login with Google</a>
        
        <% } %>
      </div>
      
    </nav>